# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
*.db
*.db-journal

# Node
node_modules/
.next/
out/
build/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Logs (can grow huge — backfill_weekly.log hit 210MB and broke GitHub push)
*.log
backend/*.log

# Scraped data dumps (transient build artifacts, not source)
backend/data/scraped/

# Local mirror of every set logo across all locales (safety net
# before R2 migration — see backup_all_set_logos.py). ~85MB of
# binaries; not something to shove through git.
backend/data/logo_backup/

# LO's local mascot working copies (we use the renamed ones in code)
frontend/public/*.apng
frontend/public/pullist-mascot-*-backup.png

frontend/public/pull.apng
frontend/public/sleeping.apng
frontend/public/normal.apng
frontend/public/fly.apng

# Local JP image archive — backup, not for repo
backups/
