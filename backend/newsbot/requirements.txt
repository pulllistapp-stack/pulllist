httpx>=0.27,<1.0
pydantic>=2.5,<3
pydantic-settings>=2.0,<3
anthropic>=0.40,<1.0
tavily-python>=0.5
# Scrapling [fetchers] pulls in curl_cffi (for plain stealth) + Playwright
# (for AsyncStealthySession with solve_cloudflare=True). The 'scrapling
# install' CLI command — run in CI before main.py — fetches the Chromium
# binary Playwright needs (~300MB; cached by actions/setup-python).
scrapling[fetchers]>=0.3
